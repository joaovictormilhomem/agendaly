# Ambiente só para `npm test` — use um banco dedicado (não o de desenvolvimento).
# Crie no Postgres: CREATE DATABASE agendaly;
# O usuário precisa de USAGE/CREATE no schema public nesse banco (igual ao dev).

TZ=UTC
PORT=3333
HOST=127.0.0.1
LOG_LEVEL=error
APP_KEY=test-app-key-at-least-32-characters
APP_URL=http://127.0.0.1:3333

DB_CONNECTION=pg
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=agendaly
DB_PASSWORD=apjf834ab
DB_DATABASE=agendaly

JWT_ACCESS_SECRET=test-jwt-access-secret-32chars-min
JWT_REFRESH_SECRET=test-jwt-refresh-secret-32chars-min

SESSION_DRIVER=memory
